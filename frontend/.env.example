# Copy to .env and adjust if your backend runs elsewhere. Keep it same-SITE with
# the SPA (localhost:5173) or the SameSite=Strict refresh cookie won't be sent —
# so `localhost`, not `127.0.0.1`.
VITE_API_URL=http://localhost:8000
