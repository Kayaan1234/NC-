import Figure from '../../components/Figure'
import Neuron from '../diagrams/Neuron'

# Overview

<Figure
  n={1}
  caption="A single neuron. Each input is scaled by its own weight, the results are summed together with a bias, and the total is passed through a nonlinearity to give one output."
>
  <Neuron />
</Figure>

This is the most simple concept to build, and one that you should visualise when building more complex models. 
A forward pass of a neuron takes a weighted sum of the inputs, applies a bias term and then a nonlinearity. 
A non-linearity may seem unnecessary at this point, but when we get to building more complex models, 
you will see it is the defining characteristic of neural computing. Without a nonlinearity, 
a large multi-layered neural network can be simplified to a single neuron, as a linear combination of 
linear functions also results in a linear function. The forward pass ends by scoring that output against the true 
label with a loss function, in this case binary loss. The backward pass then differentiates it. Since the loss is a 
function of the weights and biases, we can use our calculus skills to minimise the loss by taking partial derivatives 
(the loss with respect to each of the weights and the bias), and then adjusting these values by "surfing" down the gradient.
