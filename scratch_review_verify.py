# Throwaway file to verify the Claude PR review workflow actually posts
# comments after the allowedTools fix. Will be deleted before merge.


def average(numbers):
    total = 0
    for n in numbers:
        total += n
    return total / len(numbers)
