fastapi
uvicorn
pydantic
pydantic-settings
sqlalchemy
email-validator
bcrypt
pyjwt
scalar-fastapi
# Postgres driver (used when DATABASE_URL points at postgresql://)
psycopg[binary]
# Schema migrations for the Postgres cutover
alembic
resend
starlette
slowapi
# Shared rate-limit counter storage in prod (RATE_LIMIT_STORAGE_URI=redis://...);
# imported lazily by `limits`, so memory:// dev doesn't need it. <8 per limits.
redis
# SQLAlchemy Vector type for the pgvector column (bridge feasibility engine);
# pure-Python, tiny, needed by both web and worker since models/ is imported at boot.
pgvector